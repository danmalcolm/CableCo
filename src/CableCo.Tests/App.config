<?xml version="1.0" encoding="utf-8" ?>
<configuration>
	<connectionStrings>
        <!-- Connection used to create test database during test run (note connection timeout - LocalDB can take a while to start when first accessed)-->
        <add name="setup" connectionString="Server=(localDB)\v11.0;Initial Catalog=master;Integrated Security=SSPI;Connection Timeout=60" />
		<add name="accounts" connectionString="Server=(localDB)\v11.0;Initial Catalog=CableCo.Accounts.IntegrationTests;Integrated Security=SSPI" />
	</connectionStrings>
</configuration>